# Template for terraform.tfvars:
#
#   cp terraform.tfvars.example terraform.tfvars
#
# terraform.tfvars is gitignored.
#
# Only the variables without a default are required. Everything else has one in
# variables.tf.

cloudflare_api_token = "..."
cloudflare_zone_id   = "..."
